/* eslint-disable */
/**
 * THIS FILE IS A STUB. Run `npx convex dev` once to replace it with the
 * real auto-generated file with strict typed function references.
 *
 * The stub uses `any` casts so the Next.js build can compile before the
 * Convex deployment is bootstrapped. Function references still work at
 * runtime once the deployment is live, since they are looked up by path.
 */
import type { FunctionReference } from 'convex/server';
import { anyApi } from 'convex/server';

export const api: any = anyApi;
export const internal: any = anyApi;

export type AnyApi = typeof anyApi;
export type AnyFunctionReference = FunctionReference<any>;
